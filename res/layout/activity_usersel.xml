<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".UserSelActivity" >

    <TextView
        android:id="@+id/likeHead"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/guessyoulike" />

    <TextView
        android:id="@+id/Ilike"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/likeHead"
        android:text="@string/Ilike"
        android:layout_marginLeft="140dp" />

    <ImageButton
        android:id="@+id/makebtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@null"
        android:src="@drawable/makebtn" />
    <ListView
        android:id="@+id/spotList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/makebtn"
        android:layout_alignLeft="@id/likeHead"
        android:layout_below="@id/likeHead"
        android:layout_marginTop="10dp" >
    </ListView>

</RelativeLayout>